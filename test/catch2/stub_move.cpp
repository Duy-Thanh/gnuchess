#include "frontend/common.h"

// frontend/move.cc

void MakeMove (int side, int *move) {}
void UnmakeMove (int side, int *move) {}
void SANMove (int move, int ply) {}
leaf * ValidateMove (char *s, char *cleanMove) { return 0; }
